# 数据目录

## 结构
- `raw/` – 原始数据文件（如扫描件、截图），仅用于存档，不保证结构。
- `processed/` – 经过清洗的结构化数据，可直接用于分析。
- `external/` – 外部数据源的链接或说明。

## 当前重点
克鲁克斯辐射计温差驱动旋转数据。

## 数据提交
请勿直接上传至 `raw`，应先在 `processed` 中创建符合规范的 CSV 文件，并附上来源说明。