# 优化计划

当前快照：**核心理论成熟、外围扩散广、但验证和接入深度不均匀**。
下一阶段：从"广度扩散"转向"深度锁定"。

---

## 核心定位（修正）

SPUM-core 不是"给子项目提供 API 的中心仓库"——它的核心使命是：**让 AI 具备 ⟨P, ε⟩ 世界观**。

- 规则文件（`.trae/rules/`）是世界观的定义
- AI 加载规则后，在 SPUM 框架下推理、开发、决策
- 子项目是基于这个世界观开发的产物，不是 SPUM-core 的"客户"

项目成熟后可独立演化，从 SPUM-core 中分离为独立仓库——但那是一个自然的分娩过程，不是设计目标。

---

## 子项目清单

当前 SPUM-core 孵化的两个成熟子项目：

| 项目 | 目录 | 定位 | 状态 |
|------|------|------|:----:|
| **青檬种子** | `seed/` | 图认知 AI —— 以 1155 汉字为节点，通过漫步演化自动涌现概念结构，具备自主对话能力的拓扑认知系统 | 24873 帧训练，已验证超级簇垄断问题 |
| **青囊** | `五行/地元/青囊/` | 中医临床智能体 —— SPUM 五形框架下的辅助诊断与治疗方案生成系统，覆盖 7 部经典 SPUM 归约，含 PPG 数字脉诊硬件管线 | 已上线，有真实病历 |

两个项目的共同特征：
- 有自己的 SKILL.md，世界观由 SPUM-core 规则定义
- 不依赖 SPUM-core 的 Python 代码（代码自成体系）
- 产生了真实数据（青檬：700+ 训练日志；青囊：病历/脉诊波形）
- 成熟后可独立仓库管理，与 SPUM-core 保持世界观同步即可

---

## 帧 1：AI 世界观闭环（优先级最高）

> **让加载 SPUM 规则的 AI 真正做到"带着 ⟨P, ε⟩ 世界观推理"，而不是仅在词汇层面模仿。**

| 子项 | 状态 | 说明 |
|------|:----:|------|
| 1.1 推理闭环代码化 | ⏸️ | `spum/review.py` + `frame.py` 已定义评审-回退机制，但 AI 推理时评审评分→低分回滚→快照链回溯的全闭环未跑通 |
| 1.2 反例库自检 | ✅ 定义 | `spum-anti-pattern.md` + `spum-anti-examples.md` 已完备，但 AI 在每轮推理后是否自动执行自检？当前靠人提醒 |
| 1.3 世界观一致性审查 | ⏸️ | 同一 AI 在不同 session 的推理结果是否保持 ⟨P, ε⟩ 一致性？缺交叉 session 审计 |

**关键判断**：如果 1.1 跑通了，SPUM 就从一个"理论框架"变成了一个"自修正的推理引擎"——定位完全不同。

---

## 帧 2：实证锚点定量化加固

> 每一个没有定量验证的悬挂端，都要闭环。

目前唯一有强定量拟合的是**子图协动 → SPARC 175 星系旋转曲线**（3 参数，94.3% 有效，χ² 中位数 0.68）。这是整个项目对外最硬的"钉子"。以下三个管道需要同等力度的定量验证：

| 管道 | 现状 | 目标 | 难度 |
|------|------|------|:----:|
| **经济学** | `wuxing_predict.py` + `vis_data.json` 雏形，五形策略未跑赢布林带 | A 股全市场回测框架，夏普/最大回撤/IC 序列 vs 传统指标 | ⭐⭐⭐ |
| **光谱** | `p1_光谱指纹子网.py` + `p2_σ梯度与谱线偏移.py` 框架完成，缺真实数据验证 | SDSS 真实光谱上 σ 红移 vs z 红移的复现对比 | ⭐⭐⭐⭐ |
| **脉诊** | PPG 算法 + PTBXL 数据集分析，管线可运行 | PPG 信号→S 向量 pipeline 标准化，临床验证 | ⭐⭐ |

---

## 帧 3：新人首帧入口

> 仓库当前对新人极度不友好。几百个文件，无从下手。

| 子项 | 说明 |
|------|------|
| 3.1 Jupyter 教程 | `pip install -e .` → 创建小图 → 跑一帧演化 → 观察悬挂端残留 → 观察 σ 变化。5 分钟"摸到" SPUM |
| 3.2 交互式可视化 | 3D 粒子网络演化演示（WebGL），渲染 κ 网络本身而非数值表格 |
| 3.3 精简入口 | "只看这 3 个文件"——对应 AGENT.md 的"先别读规则文件。跑这个。" |

---

## 帧 4：跨领域一致性维护

> 当底层公理调整时，如何确保下游所有文件同步？

| 子项 | 状态 | 说明 |
|------|:----:|------|
| 4.1 推导漂移检测 | ✅ 已有 | `tools/check_derived_drift.py`，检查范围有限 |
| 4.2 公理变更影响图 | ⏸️ | 从 `network/edges.txt` 出发，标注每个下游文件依赖哪些公理。公理修订时自动列出待审计文件清单 |
| 4.3 悬挂端扫描 | ⏸️ | 81 章道德经释义 + 60+ 卦象 + 10+ 部经典 SPUM 映射 + 各领域文件——理论调整时都是潜在悬挂端。自动化扫描能大幅降低维护负担 |

---

## 帧优先级总表

| 帧 | 内容 | 优先级 | 耗时估计 |
|:--:|------|:------:|:--------:|
| 1 | AI 世界观闭环 | 🔴 最高 | 中 |
| 2 | 实证锚点定量化 | 🔴 高 | 长 |
| 3 | 新人首帧入口 | 🟡 中 | 中 |
| 4 | 跨域一致性维护 | 🟢 基线 | 持续 |
